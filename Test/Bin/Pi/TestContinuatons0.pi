# test continuations

# make a continuaton that just adds one to the
# top of stack
{ 1 + } 'op #

2 op & 3 == assert
3 op & 4 == assert

# same thing, but adds a "foo" string
{ "foo" + } 'addfoo #

"bar" addfoo & "barfoo" == assert

# locals
{ 'a # 'b # a b + } 'add #

1 2 add & 3 == assert

"foo" "bar" add & "foobar" == assert




