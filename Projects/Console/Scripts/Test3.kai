fun p(a)
	Printf("a=#0", [a])
	
fun r(a)
	Print(a)
	if a > 0
		r(a - 1)
	
fun fact(n)
	if n == 0 || n == 1
		return 1
	res = mul(n, fact(n - 1))
	Printf("fact(#0) = #1", [n, res])

fun mul(a,b)
	return a*b


