projs="Library Executor Console Network NetworkGen"

for i in $projs
do
	local="Tests/$i/Makefile.local"
	echo Replacing Tests/$i/Makefille
	cat Makefile.TestGeneric | sed "s/XXX/$i/" > Tests/$i/Makefile
	if [ ! -e $local ] 
	then
		echo $local
		touch $local
	fi
done



